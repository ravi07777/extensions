// use an integer for version numbers
version = 1

cloudstream {
    // Plugin display name in CloudStream
    description = "OlaMovies — Bollywood, Hollywood, South Indian, Hindi Dubbed & Web Series in HD/4K. Auto-resolves domain so it never breaks."
    authors = listOf("CloudStream User")

    /**
     * Status: 1 = working, 2 = slow, 3 = broken
     */
    status = 1

    // Set to true if the extension contains adult content
    adult = false

    // Icon URL (optional, shown in extension list)
    iconUrl = "https://olamovies.app/favicon.ico"
}

